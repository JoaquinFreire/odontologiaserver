# Copy this file to .env and fill with real values before running locally
# Never commit your real .env with secrets to the repo.

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your_jwt_secret_here

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=odontologia_db

# Optional
# ALLOWED_ORIGINS=https://odontologiahi.com,https://api.odontologiahi.com
